# The API's origin — no trailing slash, no /api suffix. The client appends the paths.
# Vercel bakes this in at BUILD time, so changing it needs a redeploy, not a restart.
VITE_API_URL=http://localhost:5001
