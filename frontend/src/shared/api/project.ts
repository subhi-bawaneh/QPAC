/**
 * The seeded QPAC project (Dip.Infrastructure/Seeding/SeedData.cs).
 *
 * Every entity already carries a ProjectId, so multi-project is a switcher away;
 * until the admin screens land in 6.6, every page reports on this one.
 */
export const QPAC_PROJECT_ID = '11111111-1111-1111-1111-111111111111'
